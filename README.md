[![Review Assignment Due Date](https://classroom.github.com/assets/deadline-readme-button-22041afd0340ce965d47ae6ef1cefeee28c7c493a6346c4f15d667ab976d596c.svg)](https://classroom.github.com/a/fPQK30A4)
#Python程序设计-凯撒密码

## 项目简介
* 本次作业旨在实现凯撒密码的功能，通过将字母表中的每个字母按照固定的位数进行移位来加密信息，实现明文的加密和密码的解密。

## 基本信息
**组员1**：
* 姓名：陈睿琳
* 学号：2244423868
* 分工：代码编写（加密解密函数）、readme文件撰写、流程图绘制

**组员2**：
*  姓名：田孟可
*  学号：2246121972
*  分工：代码编写（图形化、暴力破解算法、文件读取）、运行演示录制

## 功能实现

**图形用户界面（GUI）**：使用Python内置标准图像库tkinter，实现交互界面的优化，更加美观清晰。

**字符加密**：
  * 字符判断：实现字母与非字母、字母大小写的判断，方便分类处理。
  * 移位计算：利用ord()和chr()转换字符和ASCII码，通过取模实现循环。
  * 密钥归一：通过对密钥key取模，确保密钥可以被正确处理。

**解密实现**：直接调用加密函数，传入-key，简介且复用代码。

**暴力破解**：无需用户输入密钥，程序自动尝试所有可能的偏移量，输出所有可能结果，用户自行判断明文。

**文件读取**：用户可以直接上传.txt文件，并获得加密/解密后的文档。

**异常处理**：捕获非数字错误，提高程序的健壮性。

## 涉及知识点
* 函数及函数的调用
* 取模运算
* 条件判断

## 如何运行
**环境要求**：Python 3.6 或更高版本
**运行命令**：'Caesar_Cipher.py'

## 代码结构
```mermaid
flowchart TD
  A[开始] --> B[用户进入GUI界面]
  B --> C[功能1/2：加密/解密文本（手动直接输入）]
  B --> E[功能3/4：加密/解密文本（处理.txt文件）]
  B --> G[功能5：暴力破解]

  C --> C1[获取文本]
  C1 --> C2[获取密钥]
  C2 --（+key）--> C3[文本加密]
  C2 --（-key）--> C4[文本解密]

  C3 --> C5[输出密文]
  C4 --> C6[输出明文]

  E --> D[读取.txt文件]
  D --> F[获取密钥]
  F --（+key）--> F3[文本加密]
  F --（-key）--> F4[文本解密]
  F3 --> F5[输出为.txt文件]
  F4 --> F5[输出为.txt文件]

  G --> G1[获取密文（手动输入）]
  G --> G2[获取密文（读取.txt文件）]
  G1 --> G11[运行26种解密方式]
  G2 --> G11
  G11 --> G111[输出26种结果]
  
  ```
## 程序执行案例

本项目实现了完整的凯撒密码加解密功能，并配备了友好的可视化交互界面。以下为核心功能的操作演示：

### 1. 核心加解密与暴力破解演示
![基础加解密演示](./video1.mp4)
<video src="https://raw.githubusercontent.com/Python-Programming-2026/project1-caesar-cipher-super-awesome-team/refs/heads/main/video1.mp4
" controls="controls" width="100%"></video>

**说明**：本段视频展示了程序的核心密码学逻辑：
* **字符精确处理**：输入包含大小写字母、数字及标点符号的复杂明文，使用密钥 `3` 进行加解密。程序能够精确移位字母，并**原样保留数字和标点符号**。
* **大数取模机制**：使用密钥 `29`（26 + 3）对密文进行解密操作，依然能成功还原明文，证明了算法底层的 `% 26` 取模逻辑生效。
* **暴力破解**：演示了在未知密钥的情况下，一键穷举 1-25 的所有移位可能，帮助用户快速比对并锁定真实的明文信息。

---

### 2. 本地文件批量处理演示
![基础加解密演示](./video2.mp4)

**说明**：本段视频展示了程序为提升实用性而集成的文件 I/O 功能：
* **文本导入**：用户可通过系统的文件选择器，直接导入本地的 `.txt` 文件，内容会自动加载至输入区。
* **结果导出**：对导入的文本完成加密/解密后，可将输出区的结果一键安全保存为新的 `.txt` 本地文件，轻松实现长文本的批量加密流转。

---

### 3. 异常拦截与防呆设计演示
![基础加解密演示](./video3.mp4)

**说明**：本段视频重点展示了程序的鲁棒性（Robustness）与容错能力：
* **非法输入拦截**：当用户在必须填写数字的密钥框中，错误地输入了英文字母（如 `abc`）并执行加解密时，系统底层会捕获该异常。
* **安全提示**：程序会立即中止运算，并弹出一个友好的报错对话框提示“密钥必须是整数！”，从而完美避免了程序崩溃卡死的情况。
